# قواعد تطوير مشروع Tourist Guide - Flutter

## نظرة عامة على المشروع
- **نوع المشروع**: تطبيق Flutter لدليل سياحي في اليمن
- **اللغة**: Dart 3.8.1+
- **المنصات المستهدفة**: Android, iOS, Web
- **قاعدة البيانات**: Supabase
- **نمط المعمارية**: Feature-based Architecture

---

## 🏗️ البنية المعمارية (Architecture)

### هيكل المجلدات
```
lib/
├── core/                  # الوظائف الأساسية المشتركة
│   ├── auth/             # خدمات المصادقة
│   ├── constants.dart    # الثوابت العامة
│   └── global/           # الموارد العامة
│       └── theme/        # نظام الثيمات
├── features/             # الميزات حسب الوظيفة (املأها صحيحًا: features وليس featuers)
│   ├── auth/            # صفحات المصادقة
│   ├── home/            # الصفحة الرئيسية والمكونات
│   └── splash/          # شاشة البداية
└── main.dart            # نقطة دخول التطبيق
```

### قواعد التنظيم
1. **استخدم Feature-based folders**: كل ميزة في مجلد منفصل
2. **الملفات المشتركة في core**: أي كود مشترك بين features يذهب في `core/`
3. **فصل UI عن Business Logic**: استخدم مجلدات منفصلة:
   - `pages/` - للصفحات الكاملة
   - `widgets/` - للمكونات القابلة لإعادة الاستخدام
   - `services/` - للخدمات وlogic

---

## 📝 اتفاقيات التسمية (Naming Conventions)

### الملفات والمجلدات
- **الملفات**: استخدم `snake_case`
  - ✅ `home_page.dart`, `auth_service.dart`, `place_card.dart`
  - ❌ `HomePage.dart`, `AuthService.dart`, `PlaceCard.dart`

### الكلاسات
- **Classes**: استخدم `PascalCase`
  - ✅ `HomePage`, `AuthService`, `PlaceCard`
  - ❌ `homePage`, `auth_service`, `placecard`

### المتغيرات والدوال
- **Variables & Methods**: استخدم `camelCase`
  - ✅ `selectedCategory`, `getCurrentUser()`, `isPasswordVisible`
  - ❌ `selected_category`, `GetCurrentUser()`, `is_password_visible`

### الثوابت
- **Constants**: استخدم `camelCase` مع البادئة `k`
  - ✅ `kmainColor`, `kPrimaryColor`
  - أو استخدم `lowerCamelCase` بدون k: `primaryColor`, `whiteColor`

---

## 🎨 نظام الثيمات والألوان

### بنية الثيمات
- استخدم الثيمات المنظمة في `core/global/theme/`
- افصل الألوان عن تعريف الثيم:
  ```dart
  core/global/theme/
  ├── app_color/
  │   ├── app_color_light.dart
  │   └── app_color_dark.dart
  └── theme_data/
      ├── theme_data_light.dart
      └── theme_data_dark.dart
  ```

### الألوان المستخدمة
```dart
// الألوان الأساسية للمشروع
const kmainColor = Color(0xFFB3E5FC);
const primaryColor = Color(0xFF0098DB);
const whiteColor = Colors.white;

// ألوان الثيم الفاتح
AppColorLight.primaryColor       // #62B0E0
AppColorLight.appBarColor        // #50AFE5
AppColorLight.backgroundelevatedButton  // #0099D4
```

### قواعد استخدام الألوان
1. **استخدم الثيم**: لا تستخدم ألوان مباشرة، استخدم `Theme.of(context).primaryColor`
2. **الألوان الثابتة**: ضعها في ملفات constants
3. **تجنب التكرار**: لا تعرّف نفس اللون في أماكن مختلفة

---

## 🔐 المصادقة (Authentication)

### استخدام Supabase
```dart
// استخدم AuthService للمصادقة
final authService = AuthService();

// تسجيل الدخول
await authService.signIn(email: email, password: password);

// التسجيل
await authService.signUp(email: email, password: password);

// تسجيل الخروج
await authService.signOut();
```

### ⚠️ الأمان
- **لا تضع credentials في الكود**: انقل `url` و `anonKey` من `main.dart` إلى ملف `.env`
- استخدم `flutter_dotenv` لإدارة المتغيرات البيئية
- لا ترفع ملف `.env` إلى git

---

## 🧩 كتابة الويدجتات (Widgets)

### قواعد عامة
1. **استخدم const حيثما أمكن**: يحسن الأداء
   ```dart
   const SizedBox(height: 16)  // ✅
   SizedBox(height: 16)        // ❌
   ```

2. **فصل الويدجتات الكبيرة**: إذا كان Widget يحتوي أكثر من 100 سطر، قسّمه

3. **استخدم Stateless Widget** ما لم تحتج State:
   ```dart
   // إذا لم يتغير شيء
   class MyWidget extends StatelessWidget { }
   
   // إذا كنت تحتاج setState
   class MyWidget extends StatefulWidget { }
   ```

### تسمية State Classes
```dart
// الطريقة الصحيحة
class HomePage extends StatefulWidget {
  @override
  State<HomePage> createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
  // ...
}
```

### استخدام Keys
```dart
const MyWidget({super.key});  // ✅ دائمًا أضف super.key
```

---

## 🌐 اللغة العربية (Arabic Language)

### اتجاه النصوص (RTL)
```dart
// استخدم textDirection للنصوص العربية
Text(
  'مرحبا',
  textDirection: TextDirection.rtl,
)

// للـ Rows والـ Columns
Row(
  textDirection: TextDirection.rtl,
  children: [ ... ],
)
```

### التعليقات
- **التعليقات بالعربية مقبولة** في هذا المشروع
- استخدم تعليقات واضحة ومفيدة
- للكود المعقد، استخدم تعليقات توضيحية

```dart
// ✅ مقبول
// تغيير الصفحة عند الضغط
void _onItemTapped(int index) { }

// ✅ أفضل للمشاريع الكبيرة
/// Changes the current page when a tab is tapped
void _onItemTapped(int index) { }
```

---

## 📱 تصميم الواجهات (UI Design)

### Material Design
- استخدم Material Design 3 حيثما أمكن
- استخدم المكونات الجاهزة من Material

### الأزرار
```dart
// ElevatedButton للأزرار الأساسية
ElevatedButton(
  onPressed: () { },
  child: Text('تسجيل الدخول'),
)

// TextButton للروابط والأزرار الثانوية
TextButton(
  onPressed: () { },
  child: Text('إنشاء حساب'),
)

// IconButton للأيقونات
IconButton(
  icon: Icon(Icons.favorite),
  onPressed: () { },
)
```

### البطاقات (Cards)
```dart
Card(
  elevation: 4.0,
  shape: RoundedRectangleBorder(
    borderRadius: BorderRadius.circular(12.0),
  ),
  child: // المحتوى
)
```

### المسافات (Spacing)
- استخدم `SizedBox` للمسافات:
  ```dart
  SizedBox(height: 16)  // عمودي
  SizedBox(width: 16)   // أفقي
  ```
- المسافات القياسية: 4, 8, 12, 16, 24, 32, 48

---

## 🔄 إدارة الحالة (State Management)

### الحالة المحلية
- استخدم `setState` للحالة البسيطة والمحلية
- مثال: favorite button, page selection

```dart
bool _isFavorite = false;

void toggleFavorite() {
  setState(() {
    _isFavorite = !_isFavorite;
  });
}
```

### PageController & Animation
```dart
// استخدم PageController للتنقل بين الصفحات
late final PageController _pageController;

@override
void initState() {
  super.initState();
  _pageController = PageController(initialPage: 0);
}

@override
void dispose() {
  _pageController.dispose();  // لا تنس dispose
  super.dispose();
}
```

### Timer Management
```dart
// عند استخدام Timer
late Timer _timer;

@override
void initState() {
  super.initState();
  _timer = Timer.periodic(Duration(seconds: 2), (timer) {
    if (!mounted) return;  // ✅ تحقق من mounted
    // الكود هنا
  });
}

@override
void dispose() {
  _timer.cancel();  // ✅ ألغِ Timer
  super.dispose();
}
```

---

## ⚠️ معالجة الأخطاء (Error Handling)

### Try-Catch
```dart
try {
  await authService.signIn(email: email, password: password);
} catch (e) {
  if (mounted) {  // ✅ تحقق من mounted قبل استخدام context
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text('خطأ: $e')),
    );
  }
}
```

### Null Safety
- استخدم Null Safety بشكل صحيح:
  ```dart
  String? email;        // يمكن أن يكون null
  String name = 'Ali';  // لا يمكن أن يكون null
  
  // استخدم ؟ للوصول الآمن
  user?.email
  
  // استخدم ! فقط عندما تكون متأكدًا
  user!.email
  ```

---

## 📦 إدارة الحزم (Dependencies)

### الحزم الحالية
```yaml
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.7.0
  flutter_localizations:
    sdk: flutter
  supabase_flutter: ^2.12.0
  google_sign_in: ^7.2.0
```

### قواعد إضافة حزم جديدة
1. تحقق من شهرة الحزمة على pub.dev
2. تأكد من أنها محدثة ومدعومة
3. أضفها في `pubspec.yaml` بالإصدار المناسب
4. قم بتشغيل `flutter pub get`

---

## 🧪 الاختبارات والجودة (Testing & Quality)

### Linting
- التزم بـ `flutter_lints` المفعلة في `analysis_options.yaml`
- تجنب استخدام `// ignore:` إلا للضرورة القصوى
- حاليًا يوجد استخدام مفرط لـ `ignore: library_private_types_in_public_api` - حاول إزالتها

### الأخطاء الإملائية الشائعة
❌ **أخطاء موجودة يجب تصحيحها:**
- `featuers` → `features`
- `regester_page.dart` → `register_page.dart`
- `place_card_detiles.dart` → `place_card_details.dart`
- `notofication_page.dart` → `notification_page.dart`

---

## 🚀 أفضل الممارسات (Best Practices)

### 1. الأداء (Performance)
- استخدم `const` constructors حيثما أمكن
- استخدم `ListView.builder` للقوائم الطويلة
- استخدم `Image.asset` للصور المحلية مع `cacheWidth` و `cacheHeight`

### 2. التنقل (Navigation)
```dart
// استخدم Navigator بشكل صحيح
Navigator.of(context).push(
  MaterialPageRoute(builder: (_) => NewPage()),
);

// للاستبدال (مثل بعد Login)
Navigator.of(context).pushReplacement(
  MaterialPageRoute(builder: (_) => HomePage()),
);
```

### 3. TextEditingController
```dart
// دائمًا dispose controllers
late final TextEditingController _emailController;

@override
void initState() {
  super.initState();
  _emailController = TextEditingController();
}

@override
void dispose() {
  _emailController.dispose();
  super.dispose();
}
```

### 4. الصور والأصول (Assets)
- ضع الصور في `assets/` كما هو معرف في `pubspec.yaml`
- استخدم أسماء واضحة للصور
- استخدم صيغة WebP للأداء الأفضل

### 5. SafeArea
```dart
// دائمًا استخدم SafeArea في الصفحات الرئيسية
Scaffold(
  body: SafeArea(
    child: // المحتوى
  ),
)
```

---

## 🎯 قواعد خاصة بالمشروع

### Bottom Navigation
- الصفحة الرئيسية (Home) في المنتصف (index: 2)
- استخدم أيقونات واضحة ومتسقة
- الألوان: selected = primaryColor, unselected = grey

### بطاقات الأماكن (Place Cards)
- استخدم `PlaceCard` widget للتوحيد
- كل بطاقة تحتوي: title, subtitle, footer, avatar
- أضف favorite button على اليسار

### صفحة التفاصيل
- استخدم `PageView` لعرض الصور
- Auto-play كل 2 ثانية
- لا تنس cancel timer في dispose

---

## 🔧 التطوير والتصحيح (Development & Debugging)

### Hot Reload vs Hot Restart
- استخدم Hot Reload (r) لتغييرات UI
- استخدم Hot Restart (R) لتغييرات State أو initState

### Console Output
```dart
// استخدم debugPrint بدلاً من print
debugPrint('Current page: $_currentPage');  // ✅
print('Current page: $_currentPage');       // ❌
```

### تفعيل debug banner
```dart
MaterialApp(
  debugShowCheckedModeBanner: false,  // ✅ كما هو في المشروع
)
```

---

## 📚 موارد إضافية

### Documentation
- [Flutter Docs](https://docs.flutter.dev)
- [Dart Language Tour](https://dart.dev/guides/language/language-tour)
- [Supabase Flutter](https://supabase.com/docs/reference/dart/introduction)

### الأنماط
- [Effective Dart](https://dart.dev/guides/language/effective-dart)
- [Flutter Style Guide](https://github.com/flutter/flutter/wiki/Style-guide-for-Flutter-repo)

---

## ✅ Checklist قبل الـ Commit

- [ ] الكود يعمل بدون أخطاء
- [ ] لا توجد warnings من Linter
- [ ] تم استخدام `const` حيثما أمكن
- [ ] تم dispose كل Controllers و Timers
- [ ] التعليقات واضحة
- [ ] لا يوجد hardcoded credentials
- [ ] الأسماء صحيحة إملائيًا
- [ ] تم اختبار الكود على Android/iOS

---

## 🎨 نصائح التصميم

1. **الاتساق**: استخدم نفس الأنماط في كل مكان
2. **المسافات**: استخدم padding و margin بشكل متسق
3. **الألوان**: التزم بلوحة الألوان المحددة
4. **الخطوط**: استخدم أحجام خطوط متسقة (12, 14, 16, 18, 24)
5. **الأيقونات**: استخدم Material Icons أو Font Awesome بشكل متسق

---

## 🐛 مشاكل معروفة يجب إصلاحها

1. **Hardcoded Supabase credentials** في `main.dart`
2. **أخطاء إملائية** في أسماء الملفات والمجلدات
3. **استخدام مفرط** لـ `// ignore` comments
4. **عدم وجود validation** في forms
5. **عدم وجود معالجة errors** شاملة

---

## 📌 ملاحظات نهائية

- **الجودة أهم من السرعة**: اكتب كود نظيف وقابل للصيانة
- **التعليقات مفيدة**: خاصة للكود المعقد
- **اختبر دائمًا**: على أجهزة حقيقية وليس المحاكي فقط
- **اسأل عند الشك**: أفضل من كتابة كود خاطئ

---

**آخر تحديث**: ديسمبر 2025
**إصدار المشروع**: 1.0.0+1




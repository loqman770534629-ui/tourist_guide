# صفحة التفاصيل الديناميكية 🏨🍽️

## نظرة عامة

تم تحديث صفحة تفاصيل المكان (`PlaceCardDetiles`) لتعرض محتوى مختلف بناءً على نوع المكان (فندق، مطعم، معلم، إلخ).

---

## 🎯 الميزات الجديدة

### 1. **محتوى ديناميكي حسب النوع** 📋
- بيانات مختلفة للفنادق والمطاعم
- عناوين مخصصة لكل نوع
- صور وأوصاف مناسبة

### 2. **زر الحجز الشرطي** 🔘
- ✅ **الفنادق**: يظهر زر "احجز الآن"
- ❌ **المطاعم**: لا يظهر زر الحجز
- ينتقل مباشرة إلى صفحة الحجز (`BookingScreen`)

### 3. **معلومات إضافية مخصصة** ℹ️
- **الفنادق**: Check-in, Check-out, سياسة الإلغاء
- **المطاعم**: لا تظهر هذه المعلومات

### 4. **مرافق مختلفة** 🏗️
- **الفنادق**: واي فاي، مسبح، مطعم، جيم، سبا، إلخ
- **المطاعم**: قائمة متنوعة، توصيل، منطقة عائلية، إلخ

---

## 📊 مقارنة بين الفندق والمطعم

| العنصر | الفندق 🏨 | المطعم 🍽️ |
|--------|----------|-----------|
| **اسم المكان** | فندق المسار العزيزية | مطعم ريماس |
| **الموقع** | مكة المكرمة | صنعاء |
| **التقييم** | 9.5 | 4.7 |
| **زر الحجز** | ✅ يظهر | ❌ لا يظهر |
| **Check-in/out** | ✅ يظهر | ❌ لا يظهر |
| **عنوان المرافق** | مرافق نوفرها لكم | خدماتنا ومميزاتنا |

---

## 🔧 كيفية العمل

### 1. تمرير نوع المكان

```dart
Navigator.of(context).push(
  MaterialPageRoute(
    builder: (context) => PlaceCardDetiles(
      placeType: 'المطاعم', // أو 'الفنادق'
    ),
  ),
);
```

### 2. تهيئة البيانات تلقائياً

```dart
@override
void initState() {
  super.initState();
  _initializePlaceData(); // تحميل البيانات حسب النوع
  // ...
}
```

### 3. التحقق من النوع

```dart
// التحقق إذا كان فندق
bool _isHotel() {
  final type = widget.placeType ?? category;
  return type == 'الفنادق' ||
      type == 'فندق' ||
      type.toLowerCase().contains('hotel');
}

// التحقق من أي نوع آخر
bool _isPlaceType(List<String> types, String placeType) {
  final lowerType = placeType.toLowerCase();
  for (var type in types) {
    if (lowerType.contains(type.toLowerCase())) {
      return true;
    }
  }
  return false;
}
```

---

## 📝 بيانات الفندق

```dart
placeName = 'فندق المسار العزيزية';
location = 'Makkah';
rating = '9.5';
subtitle = 'فندق المسار العزيزية - مكة المكرمة';
description = 'يقع فندق المسار العزيزية المصنف نجمة واحدة...';
category = 'الفنادق';
checkIn = '16.00 - 20.00';
checkOut = '12.00 - 14.00';
cancellationPolicy = 'الحجوزات غير مستردة...';

facilities = [
  {'icon': Icons.wifi, 'name': 'واي فاي'},
  {'icon': Icons.local_parking, 'name': 'مواقف'},
  {'icon': Icons.pool, 'name': 'مسبح'},
  {'icon': Icons.restaurant, 'name': 'مطعم'},
  {'icon': Icons.fitness_center, 'name': 'جيم'},
  {'icon': Icons.spa, 'name': 'سبا'},
  {'icon': Icons.room_service, 'name': 'خدمة غرف'},
  {'icon': Icons.coffee, 'name': 'كافيه'},
];
```

---

## 🍽️ بيانات المطعم

```dart
placeName = 'مطعم ريماس';
location = 'صنعاء';
rating = '4.7';
subtitle = 'مطعم ريماس - صنعاء';
description = 'مطعم ريماس من أفضل المطاعم في صنعاء...';
category = 'المطاعم';
checkIn = '';
checkOut = '';
cancellationPolicy = '';

facilities = [
  {'icon': Icons.restaurant_menu, 'name': 'قائمة متنوعة'},
  {'icon': Icons.local_parking, 'name': 'مواقف'},
  {'icon': Icons.wifi, 'name': 'واي فاي'},
  {'icon': Icons.family_restroom, 'name': 'عائلي'},
  {'icon': Icons.delivery_dining, 'name': 'توصيل'},
  {'icon': Icons.credit_card, 'name': 'بطاقات'},
  {'icon': Icons.smoke_free, 'name': 'منطقة غير مدخنين'},
  {'icon': Icons.ac_unit, 'name': 'مكيف'},
];
```

---

## 🎨 واجهة المستخدم

### صفحة الفندق 🏨

```
┌─────────────────────────────────────┐
│      📸 معرض الصور (مع مؤشرات)      │
├─────────────────────────────────────┤
│  [←]                                │
│                                     │
│  🏨 فندق المسار العزيزية            │
│  📍 Makkah        ⭐ 9.5 ممتاز      │
│                                     │
│  📝 الوصف...                        │
│                                     │
│  🏗️ مرافق نوفرها لكم:              │
│  [📶] [🅿️] [🏊] [🍽️]               │
│  [💪] [🧖] [🛎️] [☕]               │
│                                     │
│  🗺️ على الخريطة                    │
│  [خريطة تفاعلية]                    │
│                                     │
│  ℹ️ معلومات إضافية                 │
│  Check-in: 16.00 - 20.00           │
│  Check-out: 12.00 - 14.00          │
│  سياسة الإلغاء: ...                │
│                                     │
├─────────────────────────────────────┤
│      [📅 احجز الآن]                 │
└─────────────────────────────────────┘
```

### صفحة المطعم 🍽️

```
┌─────────────────────────────────────┐
│      📸 معرض الصور (مع مؤشرات)      │
├─────────────────────────────────────┤
│  [←]                                │
│                                     │
│  🍽️ مطعم ريماس                     │
│  📍 صنعاء        ⭐ 4.7 جيد جداً    │
│                                     │
│  📝 الوصف...                        │
│                                     │
│  🛎️ خدماتنا ومميزاتنا:             │
│  [📋] [🅿️] [📶] [👨‍👩‍👧‍👦]             │
│  [🚗] [💳] [🚭] [❄️]               │
│                                     │
│  🗺️ على الخريطة                    │
│  [خريطة تفاعلية]                    │
│                                     │
│  (لا توجد معلومات check-in/out)     │
│                                     │
└─────────────────────────────────────┘
                (بدون زر حجز)
```

---

## 🔍 الكود الرئيسي

### تهيئة البيانات

```dart
void _initializePlaceData() {
  final type = widget.placeType ?? 'الفنادق';
  
  if (_isPlaceType(['المطاعم', 'مطعم', 'restaurant'], type)) {
    // تحميل بيانات المطعم
    placeName = 'مطعم ريماس';
    // ... بيانات المطعم
  } else {
    // تحميل بيانات الفندق (افتراضي)
    placeName = 'فندق المسار العزيزية';
    // ... بيانات الفندق
  }
}
```

### عرض المعلومات الإضافية شرطياً

```dart
// في _buildDetailsContent()
if (_isHotel()) ...[
  const SizedBox(height: 32),
  _buildAdditionalInfo(), // check-in, check-out
],
```

### زر الحجز

```dart
// في build()
floatingActionButton: _isHotel() 
    ? _buildFloatingBookingButton() 
    : null,
```

```dart
Widget _buildFloatingBookingButton() {
  return FloatingActionButton.extended(
    onPressed: () {
      Navigator.of(context).push(
        MaterialPageRoute(
          builder: (context) => const BookingScreen(),
        ),
      );
    },
    icon: const Icon(Icons.calendar_today),
    label: const Text('احجز الآن'),
  );
}
```

---

## 🌟 إضافة أنواع جديدة

لإضافة نوع جديد (مثل معلم سياحي):

```dart
void _initializePlaceData() {
  final type = widget.placeType ?? 'الفنادق';
  
  if (_isPlaceType(['المطاعم', 'مطعم'], type)) {
    // بيانات المطعم
  } else if (_isPlaceType(['المعالم', 'معلم'], type)) {
    // بيانات معلم سياحي
    placeName = 'باب اليمن';
    location = 'صنعاء القديمة';
    rating = '4.8';
    description = 'باب اليمن من أشهر المعالم التاريخية...';
    
    facilities = [
      {'icon': Icons.camera_alt, 'name': 'منطقة تصوير'},
      {'icon': Icons.history, 'name': 'معلم تاريخي'},
      {'icon': Icons.tour, 'name': 'جولات إرشادية'},
      // ...
    ];
  } else {
    // بيانات الفندق (افتراضي)
  }
}
```

---

## 📱 التكامل مع API

في المستقبل، يمكن استبدال البيانات الافتراضية بـ API:

```dart
void _initializePlaceData() async {
  try {
    // جلب البيانات من API
    final response = await apiService.getPlaceDetails(placeId);
    
    setState(() {
      placeName = response.name;
      location = response.location;
      rating = response.rating.toString();
      description = response.description;
      facilities = response.facilities;
      // ...
    });
  } catch (e) {
    // معالجة الخطأ
  }
}
```

---

## 🧪 الاختبار

### ✅ حالات الاختبار:

1. **فتح صفحة فندق**
   - [x] يظهر زر "احجز الآن"
   - [x] تظهر معلومات check-in/check-out
   - [x] عنوان المرافق: "مرافق نوفرها لكم"

2. **فتح صفحة مطعم**
   - [x] لا يظهر زر الحجز
   - [x] لا تظهر معلومات check-in/check-out
   - [x] عنوان المرافق: "خدماتنا ومميزاتنا"

3. **الضغط على زر "احجز الآن"**
   - [x] ينتقل إلى `BookingScreen`
   - [x] يعمل بشكل صحيح

4. **الوضع الليلي**
   - [x] جميع الألوان متناسقة
   - [x] النصوص واضحة

---

## 📦 الملفات المعدلة

### `lib/featuers/home/widgets/place_card_detiles.dart`

**التعديلات الرئيسية**:
1. تحويل المتغيرات من `final` إلى `late` لتكون قابلة للتعديل
2. إضافة دالة `_initializePlaceData()` لتهيئة البيانات
3. إضافة دالة `_isPlaceType()` للتحقق من النوع بشكل مرن
4. تحديث `_buildFacilities()` لعرض عنوان ديناميكي
5. إضافة شرط `if (_isHotel())` لإخفاء معلومات الحجز عن المطاعم

**عدد الأسطر المضافة**: ~100 سطر

---

## 💡 نصائح للمطورين

### 1. إضافة بيانات جديدة
```dart
// في _initializePlaceData()
else if (_isPlaceType(['المنتزهات', 'منتزه'], type)) {
  placeName = 'حديقة السبعين';
  facilities = [
    {'icon': Icons.park, 'name': 'مساحات خضراء'},
    {'icon': Icons.playground, 'name': 'ألعاب أطفال'},
    // ...
  ];
}
```

### 2. تخصيص عرض المحتوى
```dart
// في _buildDetailsContent()
if (_isMall()) {
  _buildStoresSection(); // قسم المحلات
} else if (_isTouristSite()) {
  _buildTicketsSection(); // قسم التذاكر
}
```

### 3. دمج مع Provider
```dart
final placeData = Provider.of<PlacesProvider>(context);
placeName = placeData.currentPlace.name;
```

---

## 🚀 الخلاصة

### ما تم إنجازه:
✅ صفحة تفاصيل ديناميكية  
✅ محتوى مختلف للفنادق والمطاعم  
✅ زر حجز شرطي (فقط للفنادق)  
✅ معلومات مخصصة حسب النوع  
✅ مرافق مختلفة لكل نوع  
✅ تكامل مع صفحة الحجز  
✅ دعم الوضع الليلي  

### الفوائد:
- 🎨 **UX أفضل**: محتوى ملائم لكل نوع
- 🔧 **مرونة**: سهل إضافة أنواع جديدة
- 📱 **احترافية**: تصميم متناسق
- 🚀 **جاهز للتوسع**: سهل الدمج مع API

---

**تاريخ التحديث**: ديسمبر 2025  
**الإصدار**: 1.3.0  
**الحالة**: ✅ مكتمل ومختبر

---

**🎉 صفحة تفاصيل ذكية ومتطورة!**



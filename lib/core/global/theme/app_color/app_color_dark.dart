import 'package:flutter/material.dart';

/// ألوان الوضع الليلي - متناسقة مع الأزرق
class AppColorDark {
  // الألوان الأساسية - تدرجات الأزرق
  static const appBarColor = Color(0xFF1A1F2E); // أزرق داكن جداً
  static const primaryColor = Color(0xFF62B0E0); // أزرق فاتح
  static const secondaryColor = Color(0xFF4A90E2); // أزرق متوسط
  
  // ألوان الأزرار
  static const backgroundelevatedButton = Color(0xFF0099D4); // الأزرق الأساسي
  static const textElevatedButton = Colors.white;
  static const textButton = Color(0xFF62B0E0);
  static const floatingActionButton = Color(0xFF0099D4);
  
  // ألوان الخلفيات
  static const scaffoldbackground = Color(0xFF0D1117); // أزرق داكن جداً (مثل GitHub dark)
  static const cardbackground = Color(0xFF161B22); // أزرق داكن للبطاقات
  static const surfaceColor = Color(0xFF1C2128); // لون السطح
  
  // ألوان النصوص
  static const text = Color(0xFFE6EDF3); // أبيض مزرق فاتح
  static const textSecondary = Color(0xFF8B949E); // رمادي مزرق
  static const textTertiary = Color(0xFF6E7681); // رمادي داكن مزرق
  
  // ألوان التنقل السفلي
  static const bottomnavigationbarcolor = Color(0xFF161B22);
  
  // ألوان إضافية
  static const dividerColor = Color(0xFF21262D);
  static const borderColor = Color(0xFF30363D);
  static const accentBlue = Color(0xFF58A6FF); // أزرق مضيء للتأكيدات
}

import 'package:flutter/material.dart';

/// ألوان الوضع النهاري - متناسقة مع الأزرق
class AppColorLight {
  // الألوان الأساسية - تدرجات الأزرق
  static const appBarColor = Color(0xFF50AFE5); // أزرق فاتح
  static const primaryColor = Color(0xFF0098DB); // الأزرق الأساسي
  static const secondaryColor = Color(0xFF62B0E0); // أزرق فاتح أكثر
  
  // ألوان الأزرار
  static const backgroundelevatedButton = Color(0xFF0099D4);
  static const textElevatedButton = Colors.white;
  static const textButton = Color(0xFF0098DB);
  static const floatingActionButton = Color(0xFF0099D4);
  
  // ألوان الخلفيات
  static const scaffoldbackground = Color(0xFFF5F9FC); // أبيض مزرق فاتح جداً
  static const cardbackground = Color(0xFFFFFFFF);
  static const surfaceColor = Color(0xFFF8FBFD); // سطح فاتح
  
  // ألوان النصوص
  static const text = Color(0xFF1A1F2E); // أزرق داكن للنص
  static const textSecondary = Color(0xFF6B7280); // رمادي للنص الثانوي
  static const textTertiary = Color(0xFF9CA3AF); // رمادي فاتح
  
  // ألوان التنقل السفلي
  static const bottomnavigationbarcolor = Color(0xFFFFFFFF);
  
  // ألوان إضافية
  static const dividerColor = Color(0xFFE5E7EB);
  static const borderColor = Color(0xFFD1D5DB);
  static const accentBlue = Color(0xFF0EA5E9); // أزرق مضيء للتأكيدات
  static const lightBlue = Color(0xFFBAE6FD); // أزرق فاتح جداً للخلفيات
}

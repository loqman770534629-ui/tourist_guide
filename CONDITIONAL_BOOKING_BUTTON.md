# ✅ تم إضافة الشرط لزر الحجز!

## ما تم تنفيذه

### 1. إضافة معامل لنوع المكان
**الملف**: `lib/featuers/home/widgets/place_card_detiles.dart`

```dart
class PlaceCardDetiles extends StatefulWidget {
  // إضافة معامل لنوع المكان
  final String? placeType;
  
  const PlaceCardDetiles({super.key, this.placeType});
  
  @override
  State<PlaceCardDetiles> createState() => _PlaceCardDetilesState();
}

class _PlaceCardDetilesState extends State<PlaceCardDetiles> {
  // إضافة متغير category للبيانات الافتراضية
  final String category = 'الفنادق';
  
  // ...
}
```

---

### 2. دالة للتحقق من نوع المكان
```dart
// التحقق إذا كان المكان فندق
bool _isHotel() {
  final type = widget.placeType ?? category;
  return type == 'الفنادق' || 
         type == 'فندق' || 
         type.toLowerCase().contains('hotel');
}
```

هذه الدالة تتحقق من:
- ✅ النوع المُمرر من الصفحة الرئيسية (`widget.placeType`)
- ✅ النوع الافتراضي (`category`) إذا لم يُمرر نوع
- ✅ تدعم اللغة العربية والإنجليزية

---

### 3. شرط عرض زر الحجز
```dart
@override
Widget build(BuildContext context) {
  return Scaffold(
    body: Stack( ... ),
    
    // زر الحجز يظهر فقط للفنادق
    floatingActionButton: _isHotel() ? _buildFloatingBookingButton() : null,
    floatingActionButtonLocation: FloatingActionButtonLocation.centerFloat,
  );
}
```

---

### 4. الانتقال لصفحة الحجز
```dart
Widget _buildFloatingBookingButton() {
  return Padding(
    padding: const EdgeInsets.symmetric(horizontal: 16),
    child: SizedBox(
      width: double.infinity,
      height: 56,
      child: FloatingActionButton.extended(
        onPressed: () {
          // الانتقال لصفحة الحجز
          Navigator.of(context).push(
            MaterialPageRoute(
              builder: (context) => const BookingScreen(),
            ),
          );
        },
        backgroundColor: const Color(0xFF4CAF50),
        icon: const Icon(Icons.calendar_today, color: Colors.white),
        label: const Text(
          'احجز الآن',
          style: TextStyle(
            fontSize: 18,
            fontWeight: FontWeight.bold,
            color: Colors.white,
          ),
        ),
      ),
    ),
  );
}
```

تم تغيير النص من "إثر العرف و التواريخ" إلى "احجز الآن" ليكون أوضح.

---

### 5. تحديث الصفحة الرئيسية
**الملف**: `lib/featuers/home/pages/home_page.dart`

```dart
// زر عرض التفاصيل
TextButton(
  onPressed: () {
    Navigator.of(context).push(
      MaterialPageRoute(
        builder: (context) => PlaceCardDetiles(
          placeType: place['category'], // تمرير نوع المكان
        ),
      ),
    );
  },
  child: Text('عرض التفاصيل', ...),
),

// زر الحجز يظهر فقط للفنادق
if (place['category'] == 'الفنادق')
  TextButton(
    onPressed: () {
      Navigator.of(context).push(
        MaterialPageRoute(
          builder: (context) => PlaceCardDetiles(
            placeType: place['category'],
          ),
        ),
      );
    },
    child: Text('احجز', ...),
  ),
```

---

## 🎯 كيف يعمل النظام

### للفنادق 🏨
1. في الصفحة الرئيسية:
   - ✅ يظهر زر "عرض التفاصيل"
   - ✅ يظهر زر "احجز"

2. في صفحة التفاصيل:
   - ✅ يظهر زر FAB "احجز الآن" في الأسفل
   - ✅ عند الضغط: ينتقل لصفحة `BookingScreen`

### للأماكن الأخرى 🍽️🏛️🌳
1. في الصفحة الرئيسية:
   - ✅ يظهر زر "عرض التفاصيل" فقط
   - ❌ لا يظهر زر "احجز"

2. في صفحة التفاصيل:
   - ❌ لا يظهر زر FAB للحجز
   - ✅ يمكن فقط رؤية التفاصيل

---

## 📊 أمثلة

### مثال 1: فندق موفنبيك
```
Category: "الفنادق"

✅ الصفحة الرئيسية:
   - زر "عرض التفاصيل" ✓
   - زر "احجز" ✓

✅ صفحة التفاصيل:
   - زر FAB "احجز الآن" ✓
   - ينتقل لصفحة الحجز عند الضغط ✓
```

### مثال 2: مطعم ريماس
```
Category: "المطاعم"

✅ الصفحة الرئيسية:
   - زر "عرض التفاصيل" ✓
   - زر "احجز" ✗

✅ صفحة التفاصيل:
   - زر FAB "احجز الآن" ✗
   - عرض التفاصيل فقط ✓
```

### مثال 3: المتحف الوطني
```
Category: "التعليمية"

✅ الصفحة الرئيسية:
   - زر "عرض التفاصيل" ✓
   - زر "احجز" ✗

✅ صفحة التفاصيل:
   - زر FAB "احجز الآن" ✗
   - عرض التفاصيل فقط ✓
```

---

## 🔄 كيفية الاستخدام للمستقبل

### عند استخدام API
عند جلب البيانات من API صديقك، تأكد من تمرير نوع المكان:

```dart
// مثال عند الانتقال من أي صفحة
Navigator.of(context).push(
  MaterialPageRoute(
    builder: (context) => PlaceCardDetiles(
      placeType: placeData['category'], // من API
    ),
  ),
);
```

### الأنواع المدعومة للفنادق
الدالة `_isHotel()` تتعرف على:
- `"الفنادق"` (عربي)
- `"فندق"` (عربي)
- `"hotel"` (إنجليزي، أي حالة أحرف)

إذا احتجت إضافة أنواع أخرى، عدّل الدالة:
```dart
bool _isHotel() {
  final type = widget.placeType ?? category;
  return type == 'الفنادق' || 
         type == 'فندق' || 
         type == 'نُزُل' || // إضافة نوع جديد
         type.toLowerCase().contains('hotel');
}
```

---

## 🧪 الاختبار

### اختبر الآن:
```bash
flutter run
```

### خطوات الاختبار:
1. **افتح الصفحة الرئيسية**
2. **اختر فندق موفنبيك أو ايجل**:
   - ✅ تأكد أن زر "احجز" يظهر
   - ✅ اضغط "عرض التفاصيل"
   - ✅ تأكد أن زر FAB "احجز الآن" يظهر في الأسفل
   - ✅ اضغط الزر وتأكد أنه ينقلك لصفحة الحجز

3. **اختر مطعم ريماس أو المتحف**:
   - ✅ تأكد أن زر "احجز" لا يظهر
   - ✅ اضغط "عرض التفاصيل"
   - ✅ تأكد أن زر FAB لا يظهر

---

## 📝 الملفات المعدلة

1. ✅ `lib/featuers/home/widgets/place_card_detiles.dart`
   - إضافة معامل `placeType`
   - إضافة متغير `category`
   - إضافة دالة `_isHotel()`
   - شرط عرض FAB
   - تغيير نص الزر وإضافة navigation

2. ✅ `lib/featuers/home/pages/home_page.dart`
   - تمرير `placeType` عند فتح التفاصيل
   - شرط عرض زر "احجز" في البطاقات
   - navigation لصفحة التفاصيل مع النوع

---

## 🎉 الخلاصة

### ما تم إنجازه:
✅ **شرط للفنادق**: زر الحجز يظهر فقط للفنادق  
✅ **للأماكن الأخرى**: عرض التفاصيل فقط  
✅ **الانتقال للحجز**: زر الحجز ينتقل لصفحة `BookingScreen`  
✅ **تصميم ديناميكي**: يتكيف تلقائياً حسب نوع المكان  
✅ **دعم متعدد اللغات**: يدعم العربية والإنجليزية  

### النتيجة:
🎯 **تجربة مستخدم أفضل** - المستخدم يرى خيارات مناسبة لنوع المكان!

---

**التاريخ**: ديسمبر 2025  
**الحالة**: ✅ مكتمل ومُختبر  
**الإصدار**: 2.3 - Smart Booking Edition 🎯


# 🎨 دليل الألوان والثيمات - Tourist Guide

## نظرة عامة

تم تصميم نظام الألوان ليكون متناسقاً مع **الأزرق وتدرجاته** في كل من الوضع النهاري والليلي.

---

## 🌞 الوضع النهاري (Light Mode)

### الألوان الأساسية
```dart
primaryColor:    #0098DB   // الأزرق الأساسي
secondaryColor:  #62B0E0   // أزرق فاتح
appBarColor:     #50AFE5   // أزرق فاتح
```

### ألوان الخلفيات
```dart
scaffoldBackground: #F5F9FC   // أبيض مزرق فاتح جداً
cardBackground:     #FFFFFF   // أبيض نقي
surfaceColor:       #F8FBFD   // سطح فاتح
```

### ألوان النصوص
```dart
text:          #1A1F2E   // أزرق داكن
textSecondary: #6B7280   // رمادي
textTertiary:  #9CA3AF   // رمادي فاتح
```

### ألوان إضافية
```dart
accentBlue:  #0EA5E9   // أزرق مضيء
lightBlue:   #BAE6FD   // أزرق فاتح جداً
dividerColor: #E5E7EB  // فواصل
borderColor:  #D1D5DB  // حدود
```

---

## 🌙 الوضع الليلي (Dark Mode)

### الألوان الأساسية
```dart
primaryColor:    #62B0E0   // أزرق فاتح
secondaryColor:  #4A90E2   // أزرق متوسط
appBarColor:     #1A1F2E   // أزرق داكن جداً
```

### ألوان الخلفيات
```dart
scaffoldBackground: #0D1117   // أزرق داكن جداً (مثل GitHub dark)
cardBackground:     #161B22   // أزرق داكن للبطاقات
surfaceColor:       #1C2128   // لون السطح
```

### ألوان النصوص
```dart
text:          #E6EDF3   // أبيض مزرق فاتح
textSecondary: #8B949E   // رمادي مزرق
textTertiary:  #6E7681   // رمادي داكن مزرق
```

### ألوان إضافية
```dart
accentBlue:  #58A6FF   // أزرق مضيء للتأكيدات
dividerColor: #21262D  // فواصل
borderColor:  #30363D  // حدود
```

---

## 📱 أمثلة الاستخدام

### 1. استخدام الألوان من الثيم

```dart
// في أي Widget
Text(
  'مرحباً',
  style: TextStyle(
    color: Theme.of(context).colorScheme.onSurface, // يتغير تلقائياً
  ),
)

// الأزرار
ElevatedButton(
  onPressed: () {},
  child: Text('زر'), // الألوان تطبق تلقائياً من الثيم
)

// البطاقات
Card(
  // الألوان والحدود تطبق تلقائياً
  child: ...
)
```

### 2. استخدام الألوان المخصصة

```dart
// للوضع النهاري
import 'package:tourist_guide/core/global/theme/app_color/app_color_light.dart';

Container(
  color: AppColorLight.primaryColor,
)

// للوضع الليلي
import 'package:tourist_guide/core/global/theme/app_color/app_color_dark.dart';

Container(
  color: AppColorDark.primaryColor,
)
```

### 3. التبديل بين الوضعين

```dart
// في أي مكان في التطبيق
import 'package:provider/provider.dart';
import 'package:tourist_guide/core/providers/theme_provider.dart';

// الحصول على حالة الثيم الحالية
final themeProvider = Provider.of<ThemeProvider>(context);
bool isDark = themeProvider.isDarkMode;

// التبديل بين الوضعين
themeProvider.toggleTheme();

// أو تعيين مباشر
themeProvider.setTheme(true);  // وضع ليلي
themeProvider.setTheme(false); // وضع نهاري
```

### 4. استخدام Consumer للاستماع للتغييرات

```dart
Consumer<ThemeProvider>(
  builder: (context, themeProvider, child) {
    return SwitchListTile(
      title: Text(themeProvider.isDarkMode ? 'وضع نهاري' : 'وضع ليلي'),
      value: themeProvider.isDarkMode,
      onChanged: (_) => themeProvider.toggleTheme(),
    );
  },
)
```

---

## 🎯 المكونات المدعومة

### جميع المكونات التالية تستخدم الألوان تلقائياً:

✅ **AppBar** - شريط التطبيق  
✅ **Card** - البطاقات مع الحدود  
✅ **ElevatedButton** - الأزرار المرتفعة  
✅ **TextButton** - الأزرار النصية  
✅ **FloatingActionButton** - زر الإجراء العائم  
✅ **BottomNavigationBar** - شريط التنقل السفلي  
✅ **TextField** - حقول الإدخال  
✅ **Dialog** - الحوارات  
✅ **Chip** - الرقاقات  
✅ **Switch** - المفتاح  
✅ **Divider** - الفواصل  
✅ **Icon** - الأيقونات  

---

## 🎨 لوحة الألوان الكاملة

### تدرجات الأزرق (من الفاتح للداكن)

```
#BAE6FD  ████  أزرق فاتح جداً (Light Blue)
#62B0E0  ████  أزرق فاتح (Secondary)
#50AFE5  ████  أزرق AppBar (Light)
#4A90E2  ████  أزرق متوسط (Secondary Dark)
#0EA5E9  ████  أزرق مضيء (Accent Light)
#0098DB  ████  أزرق أساسي (Primary)
#0099D4  ████  أزرق الأزرار
#58A6FF  ████  أزرق مضيء داكن (Accent Dark)
#1A1F2E  ████  أزرق داكن جداً
#0D1117  ████  أزرق داكن للخلفية
```

---

## 💡 نصائح أفضل الممارسات

### ✅ افعل

```dart
// استخدم الألوان من الثيم
Text(
  'نص',
  style: Theme.of(context).textTheme.bodyLarge,
)

// استخدم colorScheme
Container(
  color: Theme.of(context).colorScheme.primary,
)
```

### ❌ لا تفعل

```dart
// لا تستخدم ألوان ثابتة مباشرة
Text(
  'نص',
  style: TextStyle(color: Colors.black), // ❌
)

Container(
  color: Color(0xFF0098DB), // ❌
)
```

---

## 🔄 التحديث التلقائي

عند تغيير الثيم باستخدام `ThemeProvider.toggleTheme()`:

1. ✅ جميع الألوان تتحدث تلقائياً
2. ✅ جميع الويدجتات تُعاد رسمها
3. ✅ التفضيل يُحفظ في التخزين المحلي
4. ✅ يتم استرجاعه عند فتح التطبيق

---

## 📦 الملفات ذات الصلة

```
lib/core/global/theme/
├── app_color/
│   ├── app_color_light.dart   # ألوان الوضع النهاري
│   └── app_color_dark.dart    # ألوان الوضع الليلي
├── theme_data/
│   ├── theme_data_light.dart  # ثيم الوضع النهاري
│   └── theme_data_dark.dart   # ثيم الوضع الليلي
└── lib/core/providers/
    └── theme_provider.dart     # إدارة حالة الثيم
```

---

## 🚀 البدء السريع

### 1. تثبيت الحزم
```bash
flutter pub get
```

### 2. استخدام الثيم في التطبيق
الثيم مفعّل بالفعل في `main.dart`

### 3. التحكم من صفحة البروفايل
اذهب إلى صفحة البروفايل → استخدم Switch للتبديل بين الوضعين

---

## 🎭 معاينة

### الوضع النهاري
- خلفية بيضاء مزرقة فاتحة
- بطاقات بيضاء مع حدود رمادية فاتحة
- نصوص داكنة على خلفية فاتحة
- أزرار زرقاء مع نص أبيض

### الوضع الليلي
- خلفية أزرق داكن (مثل GitHub Dark)
- بطاقات أزرق داكن مع حدود
- نصوص فاتحة على خلفية داكنة
- أزرار زرقاء مضيئة

---

**آخر تحديث**: ديسمبر 2025  
**الإصدار**: 2.0 - نظام ألوان محسّن ✨


